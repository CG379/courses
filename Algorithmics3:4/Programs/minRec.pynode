def min(list):
    if len(list) <= 1:
        return list[0]
    
    low = min(list[1:])
    if list[0] < low:
        return list[0]
    else:
        return low

numbers = [2,3,6,1,2,5,78,9]

print(min(numbers))